-- Create config table
if not arpconfig then arpconfig = {} end

-- Mod Settings
arpconfig.debug_mode = true
arpconfig.vein_area = 7					-- Used to define the minimum distance allowed between ore vein spawns.
arpconfig.starting_area = 400		-- Used to define the minimum distance from the starting area before ore veins can spawn.